{
    "devDependencies": {
        "jshint": "latest",
        "archiver": "latest"
    },
    "scripts": {
        "build": "node build.js",
        "test": "jshint --exclude js/jquery.min.js ."
  }
}
